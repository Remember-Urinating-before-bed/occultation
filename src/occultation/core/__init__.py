"""Package holding the custom (non-reference) astronomical maths.

Every module here implements a textbook or standards formula from this
repository's own sources, with the printed page cited next to each constant.
Nothing here may import ``occultation.reference``.
"""
